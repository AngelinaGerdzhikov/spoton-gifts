import { Routes } from "@angular/router";
import { AuthAbstractionComponent } from "./abstraction/auth-abstraction.component";

export const routes: Routes = [
    {
      path: '',
      component: AuthAbstractionComponent
    }
];